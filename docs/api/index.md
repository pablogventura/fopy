# Package `fopy`

Top-level symbolic FO API: formulas, terms, structures, parsing, printing.

::: fopy
    options:
      show_submodules: true
      filters:
        - "!^draw$"
        - "!^finite$"
        - "!^builders$"
        - "!^universal$"
        - "!^solvers$"

Subpackages with dedicated pages: [finite](finite.md), [builders](builders.md),
[draw](draw.md), [universal](universal.md).
